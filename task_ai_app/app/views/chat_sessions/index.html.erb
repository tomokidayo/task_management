<h1>チャット一覧</h1>
<%= button_to "新しいチャットを作成", chat_sessions_path, method: :post %>
<ul>
  <% @chat_sessions.each do |session| %>
    <li><%= link_to session.title || "チャット ##{session.id}", session %></li>
  <% end %>
</ul>
